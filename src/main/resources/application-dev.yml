server:
  port: 8080
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/ltm_demo
    username: postgres
    password: 1234
  jpa:
    hibernate:
      ddl-auto: update
open:
  api:
    title: API for network programming
    version: v1.0.0
    description: demo
    serverUrl: http://localhost:${server.port}
    serverName: server dev
